# docker compose environment (copy to .env)
DB_PASSWORD=change-me-strong-password
# openssl rand -hex 48
JWT_SECRET=
TZ=Asia/Amman
TRUST_PROXY=1
ADMIN_USERNAME=admin
# Leave empty to have a random password printed once in the container log
ADMIN_PASSWORD=
